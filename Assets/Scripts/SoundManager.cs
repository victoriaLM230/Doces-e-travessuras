using UnityEngine;

public class SoundManager : MonoBehaviour
{
    [Header("SomDoCorvo")]
    public float somDoCorvo;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
